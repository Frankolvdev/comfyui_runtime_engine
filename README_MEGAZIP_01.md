# MegaZIP 01 — nota de integración

Descomprimir sobre la raíz de `comfyui_runtime_engine`.

No contiene carpeta raíz envolvente. Los archivos principales quedan directamente como:

```text
pyproject.toml
README.md
runtime-engine.example.toml
src/
tests/
```

Commit sugerido:

```bash
git add .
git commit -m "feat: add local ComfyUI runtime engine foundation"
git push
```
